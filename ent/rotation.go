// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/kevin/vigil/ent/rotation"
	"github.com/kevin/vigil/ent/schedule"
)

// Rotation is the model entity for the Rotation schema.
type Rotation struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// ShiftLength holds the value of the "shift_length" field.
	ShiftLength string `json:"shift_length,omitempty"`
	// HandoffTime holds the value of the "handoff_time" field.
	HandoffTime string `json:"handoff_time,omitempty"`
	// RotationType holds the value of the "rotation_type" field.
	RotationType rotation.RotationType `json:"rotation_type,omitempty"`
	// StartDate holds the value of the "start_date" field.
	StartDate time.Time `json:"start_date,omitempty"`
	// null = 无限期
	EndDate time.Time `json:"end_date,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RotationQuery when eager-loading is set.
	Edges              RotationEdges `json:"edges"`
	schedule_rotations *int
	selectValues       sql.SelectValues
}

// RotationEdges holds the relations/edges for other nodes in the graph.
type RotationEdges struct {
	// Schedule holds the value of the schedule edge.
	Schedule *Schedule `json:"schedule,omitempty"`
	// Participants holds the value of the participants edge.
	Participants []*User `json:"participants,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ScheduleOrErr returns the Schedule value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RotationEdges) ScheduleOrErr() (*Schedule, error) {
	if e.Schedule != nil {
		return e.Schedule, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: schedule.Label}
	}
	return nil, &NotLoadedError{edge: "schedule"}
}

// ParticipantsOrErr returns the Participants value or an error if the edge
// was not loaded in eager-loading.
func (e RotationEdges) ParticipantsOrErr() ([]*User, error) {
	if e.loadedTypes[1] {
		return e.Participants, nil
	}
	return nil, &NotLoadedError{edge: "participants"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Rotation) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case rotation.FieldID:
			values[i] = new(sql.NullInt64)
		case rotation.FieldName, rotation.FieldShiftLength, rotation.FieldHandoffTime, rotation.FieldRotationType:
			values[i] = new(sql.NullString)
		case rotation.FieldStartDate, rotation.FieldEndDate, rotation.FieldCreatedAt, rotation.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case rotation.ForeignKeys[0]: // schedule_rotations
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Rotation fields.
func (_m *Rotation) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case rotation.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case rotation.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case rotation.FieldShiftLength:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field shift_length", values[i])
			} else if value.Valid {
				_m.ShiftLength = value.String
			}
		case rotation.FieldHandoffTime:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field handoff_time", values[i])
			} else if value.Valid {
				_m.HandoffTime = value.String
			}
		case rotation.FieldRotationType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field rotation_type", values[i])
			} else if value.Valid {
				_m.RotationType = rotation.RotationType(value.String)
			}
		case rotation.FieldStartDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field start_date", values[i])
			} else if value.Valid {
				_m.StartDate = value.Time
			}
		case rotation.FieldEndDate:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field end_date", values[i])
			} else if value.Valid {
				_m.EndDate = value.Time
			}
		case rotation.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case rotation.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case rotation.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field schedule_rotations", value)
			} else if value.Valid {
				_m.schedule_rotations = new(int)
				*_m.schedule_rotations = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Rotation.
// This includes values selected through modifiers, order, etc.
func (_m *Rotation) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QuerySchedule queries the "schedule" edge of the Rotation entity.
func (_m *Rotation) QuerySchedule() *ScheduleQuery {
	return NewRotationClient(_m.config).QuerySchedule(_m)
}

// QueryParticipants queries the "participants" edge of the Rotation entity.
func (_m *Rotation) QueryParticipants() *UserQuery {
	return NewRotationClient(_m.config).QueryParticipants(_m)
}

// Update returns a builder for updating this Rotation.
// Note that you need to call Rotation.Unwrap() before calling this method if this Rotation
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Rotation) Update() *RotationUpdateOne {
	return NewRotationClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Rotation entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Rotation) Unwrap() *Rotation {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Rotation is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Rotation) String() string {
	var builder strings.Builder
	builder.WriteString("Rotation(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("shift_length=")
	builder.WriteString(_m.ShiftLength)
	builder.WriteString(", ")
	builder.WriteString("handoff_time=")
	builder.WriteString(_m.HandoffTime)
	builder.WriteString(", ")
	builder.WriteString("rotation_type=")
	builder.WriteString(fmt.Sprintf("%v", _m.RotationType))
	builder.WriteString(", ")
	builder.WriteString("start_date=")
	builder.WriteString(_m.StartDate.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("end_date=")
	builder.WriteString(_m.EndDate.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Rotations is a parsable slice of Rotation.
type Rotations []*Rotation
