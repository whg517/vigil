// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/kevin/vigil/ent/runbook"
	"github.com/kevin/vigil/ent/schema"
	"github.com/kevin/vigil/ent/team"
)

// Runbook is the model entity for the Runbook schema.
type Runbook struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Name holds the value of the "name" field.
	Name string `json:"name,omitempty"`
	// Type holds the value of the "type" field.
	Type runbook.Type `json:"type,omitempty"`
	// 触发：manual/on_incident/on_severity/on_label_match
	Trigger map[string]interface{} `json:"trigger,omitempty"`
	// 触发命中即自动执行（仅全只读诊断 Runbook 生效，含写步骤绝不自动执行）
	AutoRun bool `json:"auto_run,omitempty"`
	// 文档式 runbook 的 Markdown 内容
	ContentMarkdown string `json:"content_markdown,omitempty"`
	// 可执行步骤链
	Steps []schema.RunbookStep `json:"steps,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the RunbookQuery when eager-loading is set.
	Edges         RunbookEdges `json:"edges"`
	team_runbooks *int
	selectValues  sql.SelectValues
}

// RunbookEdges holds the relations/edges for other nodes in the graph.
type RunbookEdges struct {
	// Team holds the value of the team edge.
	Team *Team `json:"team,omitempty"`
	// Services holds the value of the services edge.
	Services []*Service `json:"services,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// TeamOrErr returns the Team value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e RunbookEdges) TeamOrErr() (*Team, error) {
	if e.Team != nil {
		return e.Team, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: team.Label}
	}
	return nil, &NotLoadedError{edge: "team"}
}

// ServicesOrErr returns the Services value or an error if the edge
// was not loaded in eager-loading.
func (e RunbookEdges) ServicesOrErr() ([]*Service, error) {
	if e.loadedTypes[1] {
		return e.Services, nil
	}
	return nil, &NotLoadedError{edge: "services"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Runbook) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case runbook.FieldTrigger, runbook.FieldSteps:
			values[i] = new([]byte)
		case runbook.FieldAutoRun:
			values[i] = new(sql.NullBool)
		case runbook.FieldID:
			values[i] = new(sql.NullInt64)
		case runbook.FieldName, runbook.FieldType, runbook.FieldContentMarkdown:
			values[i] = new(sql.NullString)
		case runbook.FieldCreatedAt, runbook.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case runbook.ForeignKeys[0]: // team_runbooks
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Runbook fields.
func (_m *Runbook) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case runbook.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case runbook.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case runbook.FieldType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field type", values[i])
			} else if value.Valid {
				_m.Type = runbook.Type(value.String)
			}
		case runbook.FieldTrigger:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field trigger", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Trigger); err != nil {
					return fmt.Errorf("unmarshal field trigger: %w", err)
				}
			}
		case runbook.FieldAutoRun:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field auto_run", values[i])
			} else if value.Valid {
				_m.AutoRun = value.Bool
			}
		case runbook.FieldContentMarkdown:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field content_markdown", values[i])
			} else if value.Valid {
				_m.ContentMarkdown = value.String
			}
		case runbook.FieldSteps:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field steps", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Steps); err != nil {
					return fmt.Errorf("unmarshal field steps: %w", err)
				}
			}
		case runbook.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case runbook.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case runbook.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field team_runbooks", value)
			} else if value.Valid {
				_m.team_runbooks = new(int)
				*_m.team_runbooks = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Runbook.
// This includes values selected through modifiers, order, etc.
func (_m *Runbook) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryTeam queries the "team" edge of the Runbook entity.
func (_m *Runbook) QueryTeam() *TeamQuery {
	return NewRunbookClient(_m.config).QueryTeam(_m)
}

// QueryServices queries the "services" edge of the Runbook entity.
func (_m *Runbook) QueryServices() *ServiceQuery {
	return NewRunbookClient(_m.config).QueryServices(_m)
}

// Update returns a builder for updating this Runbook.
// Note that you need to call Runbook.Unwrap() before calling this method if this Runbook
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Runbook) Update() *RunbookUpdateOne {
	return NewRunbookClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Runbook entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Runbook) Unwrap() *Runbook {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Runbook is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Runbook) String() string {
	var builder strings.Builder
	builder.WriteString("Runbook(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("type=")
	builder.WriteString(fmt.Sprintf("%v", _m.Type))
	builder.WriteString(", ")
	builder.WriteString("trigger=")
	builder.WriteString(fmt.Sprintf("%v", _m.Trigger))
	builder.WriteString(", ")
	builder.WriteString("auto_run=")
	builder.WriteString(fmt.Sprintf("%v", _m.AutoRun))
	builder.WriteString(", ")
	builder.WriteString("content_markdown=")
	builder.WriteString(_m.ContentMarkdown)
	builder.WriteString(", ")
	builder.WriteString("steps=")
	builder.WriteString(fmt.Sprintf("%v", _m.Steps))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Runbooks is a parsable slice of Runbook.
type Runbooks []*Runbook
