// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/kevin/vigil/ent/override"
	"github.com/kevin/vigil/ent/schedule"
	"github.com/kevin/vigil/ent/user"
)

// Override is the model entity for the Override schema.
type Override struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 覆盖起始（含）
	StartTime time.Time `json:"start_time,omitempty"`
	// 覆盖结束（不含）
	EndTime time.Time `json:"end_time,omitempty"`
	// 换班原因，如 user_a 请假
	Reason string `json:"reason,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the OverrideQuery when eager-loading is set.
	Edges               OverrideEdges `json:"edges"`
	override_user       *int
	override_created_by *int
	schedule_overrides  *int
	selectValues        sql.SelectValues
}

// OverrideEdges holds the relations/edges for other nodes in the graph.
type OverrideEdges struct {
	// Schedule holds the value of the schedule edge.
	Schedule *Schedule `json:"schedule,omitempty"`
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// CreatedBy holds the value of the created_by edge.
	CreatedBy *User `json:"created_by,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// ScheduleOrErr returns the Schedule value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e OverrideEdges) ScheduleOrErr() (*Schedule, error) {
	if e.Schedule != nil {
		return e.Schedule, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: schedule.Label}
	}
	return nil, &NotLoadedError{edge: "schedule"}
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e OverrideEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// CreatedByOrErr returns the CreatedBy value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e OverrideEdges) CreatedByOrErr() (*User, error) {
	if e.CreatedBy != nil {
		return e.CreatedBy, nil
	} else if e.loadedTypes[2] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "created_by"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Override) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case override.FieldID:
			values[i] = new(sql.NullInt64)
		case override.FieldReason:
			values[i] = new(sql.NullString)
		case override.FieldStartTime, override.FieldEndTime, override.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		case override.ForeignKeys[0]: // override_user
			values[i] = new(sql.NullInt64)
		case override.ForeignKeys[1]: // override_created_by
			values[i] = new(sql.NullInt64)
		case override.ForeignKeys[2]: // schedule_overrides
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Override fields.
func (_m *Override) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case override.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case override.FieldStartTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field start_time", values[i])
			} else if value.Valid {
				_m.StartTime = value.Time
			}
		case override.FieldEndTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field end_time", values[i])
			} else if value.Valid {
				_m.EndTime = value.Time
			}
		case override.FieldReason:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field reason", values[i])
			} else if value.Valid {
				_m.Reason = value.String
			}
		case override.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case override.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field override_user", value)
			} else if value.Valid {
				_m.override_user = new(int)
				*_m.override_user = int(value.Int64)
			}
		case override.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field override_created_by", value)
			} else if value.Valid {
				_m.override_created_by = new(int)
				*_m.override_created_by = int(value.Int64)
			}
		case override.ForeignKeys[2]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field schedule_overrides", value)
			} else if value.Valid {
				_m.schedule_overrides = new(int)
				*_m.schedule_overrides = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Override.
// This includes values selected through modifiers, order, etc.
func (_m *Override) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QuerySchedule queries the "schedule" edge of the Override entity.
func (_m *Override) QuerySchedule() *ScheduleQuery {
	return NewOverrideClient(_m.config).QuerySchedule(_m)
}

// QueryUser queries the "user" edge of the Override entity.
func (_m *Override) QueryUser() *UserQuery {
	return NewOverrideClient(_m.config).QueryUser(_m)
}

// QueryCreatedBy queries the "created_by" edge of the Override entity.
func (_m *Override) QueryCreatedBy() *UserQuery {
	return NewOverrideClient(_m.config).QueryCreatedBy(_m)
}

// Update returns a builder for updating this Override.
// Note that you need to call Override.Unwrap() before calling this method if this Override
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *Override) Update() *OverrideUpdateOne {
	return NewOverrideClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the Override entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *Override) Unwrap() *Override {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: Override is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *Override) String() string {
	var builder strings.Builder
	builder.WriteString("Override(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("start_time=")
	builder.WriteString(_m.StartTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("end_time=")
	builder.WriteString(_m.EndTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("reason=")
	builder.WriteString(_m.Reason)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Overrides is a parsable slice of Override.
type Overrides []*Override
